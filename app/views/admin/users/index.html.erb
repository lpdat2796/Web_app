<div class="container">
  <div class="row">
    <div class="col-lg-12 text center">
      <h1 class="title">USERS MANAGEMENT</h1>
  </div>
  <div class="col-lg-12 form-group">
      <label for="">List Users</label>
      <table class="table">
        <thead>
          <th>Id</th>
          <th>Email</th>
          <th>Name</th>
          <th>Role</th>
          <th>Edit</th>
          <th>Delete</th>
        </thead>
        <tbody>
          <% @user.each do |user| %>
          <tr>  
            <td><%= user.id %></td>
            <td><%= user.email %></td>
            <td><%= user.name %></td>
            <td>
              <% if user.role == 0  %>
                User
              <% else %>
                Admin
              <% end %>
            </td>
            <td><%=link_to "Edit", admin_edit_path(id: user.id)  %></td>
            <td><%=link_to "Delete", admin_user_path(user), method: :DELETE, data: {confirm: 'Are you sure?'}  %></td>
          </tr>  
          <% end %>  
        </tbody>
      </table>
    </div>
  </div>
</div>